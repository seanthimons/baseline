# Examples

These are standalone workflows to copy into a repo's `.github/workflows/`, not reusable workflows to call.
Edit the copy as needed; nothing here is referenced via `uses:` from other repos.

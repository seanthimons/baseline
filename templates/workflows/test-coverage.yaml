# Caller for the baseline test coverage workflow. Copy to .github/workflows/.
name: Test coverage

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: test-coverage-${{ github.ref }}
  cancel-in-progress: true

jobs:
  coverage:
    name: Coverage
    permissions:
      contents: read
      id-token: write
    uses: seanthimons/baseline/.github/workflows/test-coverage.yaml@df028b5dcb494da6209f447183823492e6a44ac2 # v1.1.0
    with:
      codecov: false
      quarto: false

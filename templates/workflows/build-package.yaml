# Caller stub: build the R source package on demand via the baseline reusable workflow.
# Copy to .github/workflows/build-package.yaml in your repo.
name: Build package

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build package
    uses: seanthimons/baseline/.github/workflows/build-package.yaml@df028b5dcb494da6209f447183823492e6a44ac2 # v1.1.0
    with:
      quarto: false
      pre-build-script: ''

# Caller stub: build the R source package on demand via the baseline reusable workflow.
# Copy to .github/workflows/build-package.yaml in your repo.
name: Build package

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build package
    uses: seanthimons/baseline/.github/workflows/build-package.yaml@0a2a80a6d045ffbdacedd59e35dce1497e2acfc1 # v1.0.0
    with:
      quarto: false
      pre-build-script: ''
